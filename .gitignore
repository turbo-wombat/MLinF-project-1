.ipynb_checkpoints/
__pycache__/
*.pyc
.DS_Store
.env
venv/
data/
.claude/
# PROMPT.md §5: results.pkl exceeds the ~5 MB threshold (16.8 MB with the
# full RESULTS dict of weight/return tuples for all 15 strategies), so it
# is regenerated by running the notebook rather than committed.
results.pkl

